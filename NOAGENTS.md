## 关于工作流程的要求

不要在任务过程中反复查看无关文件、反复检查验证，这会把时间切碎浪费掉。请按下面的节奏推进。

**开工前一次性把文件看完。** 该看的文件在动手之前就全部看完，看完的产出是一份文档，放进 `docs` 里。文档要足够完整，包含你后续改动所需的全部信息——涉及哪些文件、各自的现状与职责、要改成什么样、字段与术语的对应关系、有什么你需要对照着看的技术知识等等。

**要问的问题在写文档之前一次问完。** 技术选型、设计取舍、以及你读不懂的代码，我都可以给你答复。但提问必须满足一个门槛：**只有那些确实关键、且你私自决定会造成严重后果的问题才值得问。** 具体排除三类无效提问。

第一类是**已在需求中明确的内容**。我给出的需求已经构成决策，不要再就已明确的部分反问"这个可以做但是……"之类，那不是提问而是重复。

第二类是**实现细节层面的选择**。当需求已经指定了要用的机制或对象时，该机制内部具体怎么实现属于你的职责范围，自行判断并在文档中记录理由即可，不要拿来问我。

第三类是**从零启动类的问题**。这个项目已有既定的技术栈、版本、架构和既有代码，凡是能通过阅读现有项目直接确定的前提条件，一律自己查清，不要当作待决策项提出。

同时，回答和汇报中不要夹带无关表述。我只关心你要做什么，不值得说的内容一概省略。

**改造阶段只盯文档。** 有了这份文档之后，改动过程中只对着文档写代码，不要再回头零散地翻源文件、也不要中途穿插验证。

**验证只在最后做一次。** 改完之后统一验证一次，编译检查一次，确认通过。

**最终把正确的代码交付给我。** 不要把中间态、未编译通过的版本发过来，只交付最终确认无误的结果。

## 关于如何修改的要求

修正即回归目标态，不留修正痕迹

当你的产出偏离了需求（多做、错做、加入了未被要求的内容），我指出后，你要做的是**直接产出符合原需求的结果**，而不是产出一个"标注了修正历史"的变体。

具体禁止三件事。

**第一，禁止在命名上留痕。** 不要出现"去掉了 X 的版本""无 X 版""修正版""v2"这类命名，包括文件名、类名、函数名、变量名、配置项。原需求叫什么，成果就叫什么，回到该有的名字上。

**第二，禁止在代码与文档中留痕。** 不要写注释或文档说明"这里原本有 X，现已移除""为什么不包含 X"。多余的东西被移除后，成果应当看起来就像它从一开始就没有过，不保留兼容层、开关、废弃标记或空实现。

**第三，禁止解释。** 不要说明为什么要去掉、去掉后有什么影响、以及成果为何不含该部分。我只需要正确的结果。

判断标准：**最终产物应当与"你第一次就做对"的结果完全一致**，从中看不出曾经存在偏差和修正过程。


