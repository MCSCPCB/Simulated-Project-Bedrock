## 1.遵循既有风格

严格遵守项目现有的架构分层、目录结构、命名习惯、编码风格与编码规范。
新增代码在风格上应当与周边代码不可区分，不要引入本项目未使用的写法、
新的抽象层次或新的组织方式。

## 2.禁止重复造轮子，参考项目内已有的同类链路

动手前先在项目中查找是否已有可用的实现，包括数据结构、坐标变换、几何
算法、空间索引、缓存机制、键值编码、工具函数等。已存在的一律直接复用，
不得另起一套并行实现，也不得为同一职责保留两份代码。

如果既有实现不能完全满足需求，优先考虑扩展或参数化现有实现；只有在确认
无法复用时才新写，必要的话可以向用户提问，并在交付说明中指出你新写了什么、以及为什么无法复用
现有的哪一个。

本项目中已经实现的相关功能链路是首要参考对象，其中包含了已经经过验证的
处理方式。遇到设计选择时，先看项目里同类问题是怎么解决的，按同样的方式
处理，而不是自行设计一套新方法。

## 3.不写防御性冗余代码，以最简且规范的实现为准

不要堆砌无意义的防御性逻辑，具体包括：不为不可能出现的输入写兜底分支；
不做已由上游保证的重复校验；不写空的 try/catch 或吞掉异常；不为「以防
万一」保留死代码、无用参数、无读取点的字段；不加当前用不到的配置项、
开关或扩展点。

约束条件在文档中已经明确的部分，直接按约定实现，不要再为「万一约定不成立」
补一层保护。

在满足需求的前提下取最简实现。不做过度设计、不预留未来可能用到的能力、
不为单一调用点引入抽象。简单不等于随意——命名、分层、职责划分仍须符合
项目规范，不允许用「简化」为由写出临时性、一次性或与项目风格割裂的代码。

判断标准：如果一段代码删掉之后功能不变，它就不该存在。

## 4.禁止做的事情

只有必须由生成器批量产出的代码才允许由程序生成；其余所有文件（包括 JSON 等配置文件）一律通过手写维护，禁止用脚本或程序生成或改写。

对用户手动修改过的文件，必须先询问并获得确认，才能再次修改。

## 5.如何获得fancy适配所需要的原版方块相关数据

访问以下目录，这里包含基岩版所有方块的所有信息和数据，如果你在工作过程中发现有某种方块在这里面也找不到的信息或数据，则那个方块的适配可以暂时搁置，工作后及时汇报给我。
D:\Software\Games\MINECRAFT\MCBE Addon Dev Project\Vibe_Coding_Project\Simulated-Project-Bedrock\.sample\VanillaBlock